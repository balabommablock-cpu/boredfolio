import ManagerDirectoryPage from "@/views/ManagerDirectoryPage";
export default ManagerDirectoryPage;
export const metadata = { title: "Fund Manager Directory", description: "Know who manages your money. Track records, alpha, tenure." };
