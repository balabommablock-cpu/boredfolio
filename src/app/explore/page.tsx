import { Metadata } from "next";
import ExplorePage from "@/views/ExplorePage";

export const metadata: Metadata = {
  title: "The Screener — Find Funds Worth Your Money",
  description: "Filter, compare, and roast mutual funds. Pre-built screens for consistent compounders, hidden gems, and funds to avoid.",
};

export default ExplorePage;
