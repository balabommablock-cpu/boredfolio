import { Metadata } from "next";
import ComparePage from "@/views/ComparePage";

export const metadata: Metadata = {
  title: "Fund Comparison — Three Funds Enter, One Survives",
  description: "Side-by-side mutual fund comparison with overlap analysis, risk metrics, and the Boredfolio verdict.",
};

export default ComparePage;
